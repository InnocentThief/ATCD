import React from 'react'
import { RouteComponentProps } from 'react-router'
import { Context } from '../../contexts'



class Mappers extends React.Component{
    render() {
        const{

        } = Context

        return(
            <>I'm the mappers page</>
        )
    }
}

export default Mappers